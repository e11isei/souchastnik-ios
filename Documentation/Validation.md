# Проверка iOS-порта

## Автозагрузка: проверка 11 сентября 2026

- `swift test`: 26 тестов, из них 1 сетевой пропущен по умолчанию; 25 выполнены без ошибок. Покрыты согласованность версии словаря, неполные пакеты, кеширование, проверка GGUF/размера/SHA-256, отмена и очистка временных файлов.
- Отдельный сетевой тест выполнен 7 сентября: реально скачаны словари из GitHub и вся модель Qwen3.5-0.8B Q4_0 (563 036 064 байта) с Hugging Face; SHA-256 совпал. Тест завершился без ошибок за 249,716 секунды.
- Debug для iOS Simulator и Release для iPhone собираются без подписи.
- Запуск интерфейса на симуляторе или физическом устройстве и сквозной ИИ-разбор не проверены.

## Выполнено 7 сентября 2026

- Xcode 26.2: Debug для arm64 iOS Simulator — BUILD SUCCEEDED.
- Xcode 26.2: Release для iPhone — BUILD SUCCEEDED, без подписи.
- `swift test`: 15 тестов, 0 ошибок.
- Проверены plist/entitlements/privacy manifests, shell-синтаксис и whitespace diff.
- В расширении нет зависимости от llama.cpp.
- Запуск и визуальная проверка интерфейса не выполнены: запущенного симулятора не было. Проверки на физическом устройстве и сквозного ИИ-разбора также не было.

## Автоматически

- `swift test`: синтетические тесты ядра без оригинальных приватных данных.
- Debug iOS Simulator build и Release iphoneos build без подписи.
- Расширение не должно зависеть от llama.framework; он встроен только в основное приложение.
- `bash scripts/bootstrap.sh` готовит зависимости закреплённой версии. Повторный запуск не пересобирает готовый XCFramework.
- `./start.sh` выполняет подготовку, генерацию проекта, сборку Simulator и открывает Xcode одной командой.
- При первом входе без GGUF приложение скачивает закреплённую Qwen3.5-0.8B Q4_0 с Hugging Face. Словари получаются из `main` репозитория; встроенный пакет используется как офлайн-резерв.

## Ручная приёмка на iPhone/iPad

Эти шаги не заменяются успешной компиляцией:

1. Подписать оба targets, подключить App Group, установить приложение.
2. Без полного доступа: включить клавиатуру, напечатать RU/EN, цифры, символы, ё/Ё и ъ/Ъ; проверить Shift, Caps Lock, выделение и удаление, удержание удаления, курсор на пробеле, системную кнопку 🌐.
3. Проверить портрет/альбом, тёмную/светлую тему, iPad, VoiceOver, поля с Return/Search/Send. Клавиша Return вызывает стандартный `insertText("\n")`; окончательное поведение определяет приложение-хост.
4. Переключать поля и приложения: контекст не должен сохраняться между полями. В защищённых полях должна появляться системная клавиатура.
5. На чистой установке проверить две автоматические загрузки: GitHub и Hugging Face. Дождаться готовности, перезапустить и убедиться, что модель не скачивается снова. Проверить отсутствие сети, повтор, отмену, уход в фон и возврат. После этого можно импортировать настоящий пакет, включить полный доступ и переоткрыть клавиатуру.
6. Убедиться, что триггер отображается как кандидат, а не как доказанное нарушение.
7. Импортировать неверный JSON/неизвестный код: увидеть ошибку, сохранить старый пакет. Попробовать текстовый файл вместо GGUF: получить ошибку, сохранить старую модель.
8. С оригинальными весами: проверить `none` и несколько кодов, правильность санкций из JSON, длинный ввод, отмену, изменение текста во время расчёта, уход в фон, повторный разбор после возврата.
9. Проверить скорость, память и Metal на реальном устройстве. Качество модели после переноса на Metal необходимо сравнить с исходным контрольным набором; его нет в публичном Git.

## Известные пределы

- Отсутствуют исходные словари, промпт, веса и контрольный набор. ИИ не проверен сквозным запуском с этими данными.
- Нет анализа большой модели непосредственно внутри клавиатуры: только в открытом приложении.
- Сборка Simulator рассчитана на Apple Silicon (arm64), не Intel.
- Подпись, provisioning, App Group, установка и публикация в App Store требуют настройки владельцем.
- Приложение не проверяет актуальность юридических статусов или санкций по сети. Загруженный пакет является источником этих данных.
