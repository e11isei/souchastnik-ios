# Соучастник · iOS

Нативная клавиатура для iPhone и iPad, полностью переписанная на Swift. Минимальная версия — **iOS 17**. Android-приложение, Gradle, JNI и Binder удалены; их история остаётся в Git.

Идея оригинального [«Соучастника»](https://github.com/MShverdiakov/souchastnik), автор — [@neuromikhail](https://t.me/neuromikhail): сатирическая клавиатура со словарными пометками и локальным ИИ-разбором текста. Это не юридическая консультация; модель и загруженные справочники могут ошибаться.

## Что работает

- Системное расширение клавиатуры на `UIInputViewController`: RU/EN, цифры и символы, Shift/Caps Lock, «ё»/«ъ» по удержанию «е»/«ь», удаление по словам при удержании, перемещение курсора жестом по пробелу, системная кнопка переключения клавиатур.
- Словарный поиск триггеров с нормализацией регистра, `ё` и пробельных символов. Совпадение со словарём **не выдаётся за вердикт ИИ**.
- Автоматические пометки по словарю: префиксы, точные формы, регистр, многословные имена, отдельные шаблоны для разных видов записей.
- Приложение на SwiftUI: подключение клавиатуры, настройки, импорт словарей и GGUF, поиск по справочнику, локальный разбор текста.
- Локальный llama.cpp b10726 с Qwen3.5: закрытый список ответов `none + кандидаты`, исходный `noneBias = 1.0`, контрпримеры, чат-шаблон с закрытым блоком рассуждения. Санкции берутся только из справочника.
- Отмена и отбрасывание устаревших результатов, ограничение контекста, атомарная установка пакета словарей и замена модели.
- Автоматическая загрузка модели с Hugging Face и словарей из GitHub при первом входе, с прогрессом, проверкой целостности, отменой и повтором при ошибке.

## Данные

В репозитории есть `Data/StarterPack/articles.json`, `triggers.json`, `agents.json`, `examples.json` и `judge.txt`. Это демонстрационный пакет с кодами `DEMO-1` и `DEMO-2`: он нужен для проверки импорта и интерфейса, но не является юридическим справочником. Название файла — `articles.json`.

Оригинальный автор не опубликовал настоящие словари, промпт, контрольный набор и веса модели. Поэтому стартовый пакет не выдаётся за реальные статьи и статусы. Замените файлы в `Data/StarterPack` оригинальными данными и опубликуйте их в `main`: приложение получит обновление при следующем запуске или по кнопке «Обновить словари из репозитория».

Обычная модель той же архитектуры может загрузиться, но не гарантирует качество исходного обученного варианта. Импорт проверяет заголовок GGUF; архитектура `qwen35` и загрузка контекста проверяются при первом разборе.

## Особенность iOS

В Android модель работала в отдельном сервисе с большим объёмом памяти. iOS не предоставляет расширению клавиатуры эквивалент такого фонового сервиса: расширение исполняется отдельно и ограничено по памяти. Поэтому:

| Где | Возможности |
| --- | --- |
| Клавиатура | Ввод, маркировка и словарные триггеры на последних доступных 400 символах перед курсором |
| Открытое приложение | Полный ИИ-разбор введённой или вручную вставленной фразы |

Модель не линкуется в расширение. Никакой скрытой отправки текста приложению, буфера обмена или фонового сервера нет. Для разбора откройте приложение и вставьте фразу самостоятельно. При нехватке контекста ИИ анализирует хвост текста; интерфейс сообщает об этом.

Ограничения платформы описаны в [документации Apple](https://developer.apple.com/documentation/uikit/creating-a-custom-keyboard). В защищённых полях iOS использует системную клавиатуру; некоторые приложения отключают сторонние клавиатуры. Доступный через `textDocumentProxy` контекст может быть неполным.

## Сборка

Нужны macOS, полный Xcode с iOS SDK, Apple Silicon, CMake и XcodeGen. Проект проверен компиляцией в Xcode 26.2.

```bash
brew install xcodegen cmake
./start.sh
```

`./start.sh` — единая команда: подготовить закреплённую версию llama.cpp, сгенерировать Xcode-проект, собрать Debug для iOS Simulator и открыть проект в Xcode. Если зависимости уже собраны, повторный запуск использует кэш.

При первом входе приложение автоматически скачивает модель и словари. Класть GGUF в проект или выбирать файлы вручную больше не нужно. Подробности источников и поведения — ниже.

Первый запуск `./start.sh` скачивает официальный XCFramework b10726 и исходники закреплённой ревизии, проверяет SHA-256 и собирает недостающий **arm64 iOS Simulator**-вариант. На iPhone используется официальный бинарник с Metal, в симуляторе — CPU. Сам скрипт не скачивает веса: их получает приложение при входе. `Vendor/`, `third_party/` и сборочные артефакты не включены в Git.

`project.yml` — источник структуры проекта; после его изменения выполните `xcodegen generate`. Сгенерированный `.xcodeproj` включён в репозиторий.

Для устройства в Xcode:

1. Укажите свой Team в обоих targets.
2. При необходимости замените `dev.souchastnik.ios` и `dev.souchastnik.ios.keyboard` на свои bundle IDs.
3. Зарегистрируйте общий App Group и установите **одинаковый** `APP_GROUP_IDENTIFIER` для обоих targets. Значение по умолчанию: `group.dev.souchastnik.ios`.
4. Соберите схему `Souchastnik` для iPhone/iPad. Возможность подписи расширения и App Groups зависит от вашей учётной записи Apple Developer.
5. В iOS: Настройки → Основные → Клавиатура → Клавиатуры → Новые клавиатуры → Соучастник.

Для обмена словарями/настройками между приложением и клавиатурой включите «Разрешить полный доступ». Без него ввод работает; в расширении нет словарей из приложения. Полный доступ не используется для сети: в приложении нет сетевого клиента анализа или аналитики.

## Автоматическое скачивание

При первом запуске экран появляется сразу, а две независимые загрузки выполняются асинхронно:

- **Словари:** [Data/StarterPack в вашем репозитории](https://github.com/e11isei/souchastnik-ios/tree/main/Data/StarterPack). Сначала приложение получает SHA текущего коммита `main` через GitHub API, затем скачивает все пять JSON/TXT-файлов по URL этого коммита на `raw.githubusercontent.com`. Готовый пакет заменяет старый только после полной валидации. На каждом холодном запуске проверяется версия; неизменившаяся версия не скачивается повторно. Пока сети нет, доступны сохранённые данные или встроенный демонстрационный пакет.
- **Модель:** [ggml-org/Qwen3.5-0.8B-GGUF](https://huggingface.co/ggml-org/Qwen3.5-0.8B-GGUF), Q4_0, 563 036 064 байт, ревизия `8fea620810c4afa23dd6443f999a48574c1611a3`. Проверяется SHA-256 `57d1997790d1744fba5b40a7317df71ea5e2acee28c47e78f0cce39c0703f8cf`, размер и заголовок GGUF. Файл скачивается на диск; SHA-256 считается блоками по 1 МБ. После проверки файл атомарно устанавливается в приватный контейнер приложения. Установленная модель повторно не скачивается. Это публичная инструктивная Qwen, без дообучения автора оригинальной клавиатуры.

Адреса, размер и контрольная сумма определены в `Sources/SouchastnikAssets/AssetSources.swift`. Скачивание по HTTPS, без аккаунта, API-ключа, cookies или передачи текста пользователя. Hugging Face и GitHub получают обычные запросы на скачивание файлов; сам ИИ-разбор остаётся локальным. Загрузчик входит только в приложение, расширение клавиатуры не обращается к сети.

На экранах «Клавиатура» и «Словари» отображаются прогресс, ошибка и повтор. Интернет нужен при первой установке и обновлении словаря; загрузка возможна по Wi-Fi или сотовой сети. При уходе в фон незавершённая загрузка отменяется, а после возврата начинается заново — фонового скачивания и продолжения с последнего байта пока нет. Кнопка отмены или удаление модели отключает автоматическую загрузку до нажатия «Скачать модель».

Пользовательские модели и словари, импортированные вручную, сохраняются. Возврат к словарям GitHub выполняется кнопкой «Обновить словари из репозитория». Для закрытой сборки всё ещё можно положить GGUF в bundle; он имеет приоритет перед сетевой загрузкой.

## Ручной импорт данных

Во вкладке «Словари» выберите папку с исходными файлами:

| Файл | Обязателен | Формат |
| --- | --- | --- |
| `articles.json` | Да | `articles`: массив `{code, act, title, penalty, severity}` |
| `triggers.json` | Да | `groups`: массив `{words: [String], codes: [String], clean?: String}` |
| `judge.txt` | Да | Системный промпт, содержащий `@@TABLE@@` |
| `agents.json` | Нет | `templates`/`template`, массивы `agents`/`services`; `name`, `forms`, `exact`, `strict`, `kind` |
| `examples.json` | Нет | `shots`: словарь код → массив фраз; `clean` принимается, но общие чистые примеры не добавляются в промпт, как в оригинале |

Каждый входной файл ограничен 2 МБ, общий установленный пакет — 4 МБ. Некорректный импорт не заменяет предыдущий пакет. Неизвестные служебные поля исходных JSON допускаются; установка использует только поля, нужные iOS. Исходные файлы не изменяются. После импорта переоткройте клавиатуру.

GGUF можно импортировать вручную вместо автоматической загрузки. Поддерживается и прежнее имя `libmodel-qwen35-08b-q40.so`, если внутри действительно GGUF. Модель хранится в приватном контейнере приложения, не в App Group, и исключена из резервного копирования. Исходный pipeline LoRA → f16 → imatrix → Q4_0 сохранён в `tools/make_gguf.sh`, теперь с выходом `.gguf`. Для него отдельно нужны Python ML-зависимости, адаптер и русский корпус, как в оригинальном проекте.

## Проверка

```bash
swift test
xcodebuild -project Souchastnik.xcodeproj -scheme Souchastnik \
  -sdk iphonesimulator -configuration Debug \
  -derivedDataPath build/DerivedData CODE_SIGNING_ALLOWED=NO build
xcodebuild -project Souchastnik.xcodeproj -scheme Souchastnik \
  -sdk iphoneos -configuration Release \
  -derivedDataPath build/Device CODE_SIGNING_ALLOWED=NO build
```

Тесты ядра проверяют триггеры, маркировку, совместимость данных, промпт, приоритет `none`, общие префиксы токенов и регистр. Дополнительные шаги проверки на устройстве — в [Documentation/Validation.md](Documentation/Validation.md). Проверка реального качества ИИ требует отсутствующих оригинальных данных и весов.

Тесты загрузчика проверяют закрепление версии словаря, отсутствие повторного скачивания, отказ при неполном или невалидном пакете, проверку модели и удаление временных файлов. Сетевой тест по умолчанию пропущен; для реальной загрузки 563 МБ тем же Swift-кодом выполните:

```bash
SOUCHASTNIK_NETWORK_TESTS=1 swift test --filter RemoteAssetTests/testRealDownloadsWhenExplicitlyEnabled
```

## Структура

- `iOS/App` — SwiftUI, импорт и локальный `LocalJudge`.
- `iOS/Keyboard` — самостоятельное расширение UIKit.
- `iOS/Shared` — общий контейнер словарей и настроек.
- `Sources/SouchastnikCore` — платформонезависимое Swift-ядро, без UIKit и llama.cpp.
- `Sources/SouchastnikAssets` — загрузка файлов, согласованный снимок словаря и проверка SHA-256 модели; не включается в расширение.
- `Tests` — синтетические тесты; не правовой справочник.
- `scripts` — воспроизводимая подготовка зависимостей.
- `tools` — сохранённые инструменты работы с оригинальными ИИ-данными.

## Лицензии

Исходный проект и порт — GPL-3.0, см. [LICENSE](LICENSE). llama.cpp — MIT, копия уведомления в `iOS/Resources/llama-LICENSE.txt`. Права и условия распространения импортируемых словарей, адаптеров и весов определяются их авторами отдельно.

Автоматически скачиваемая Qwen3.5-0.8B опубликована под Apache-2.0; ссылка на карточку и лицензию доступна на экране «Словари».
